import type { Plugin } from 'esbuild';
import type { Options } from '@pwrs/lit-css/lit-css';
export interface LitCSSOptions extends Omit<Options, 'css'> {
    filter: RegExp;
}
export declare function litCssPlugin(options?: LitCSSOptions): Plugin;
