{
  "name": "@websublime/vitamin-core",
  "version": "0.1.0-SNAPSHOT-42a19a2",
  "description": "Core lit functions",
  "type": "module",
  "scripts": {
    "debug": "NODE_ENV=local NODE_OPTIONS='--enable-source-maps --inspect --loader tsx' node src/any.ts",
    "dev": "NODE_ENV=development node cli.mjs --watch --serve",
    "build": "rm -rf ./dist && pnpm clean && pnpm dts && pnpm parse && pnpm types && pnpm clean",
    "parse": "NODE_ENV=production node build.mjs",
    "dts": "tsc --declaration --emitDeclarationOnly",
    "types": "mkdir dist/docs && api-extractor run --local",
    "clean": "rm -rf ./declarations && rm -rf ./temp && rm -rf ./tsconfig.tsbuildinfo",
    "lint": "eslint . --ext .js,.ts,.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@akrc/esbuild-plugin-clean": "^0.1.3",
    "@microsoft/api-documenter": "^7.22.21",
    "@microsoft/api-extractor": "^7.36.0",
    "esbuild": "^0.18.6",
    "esbuild-plugin-lit-css": "^2.0.4",
    "glob": "^10.3.0"
  },
  "dependencies": {
    "lit": "3.0.0-pre.0"
  },
  "keywords": [
    "core",
    "websublime",
    "lit",
    "ui"
  ],
  "author": {
    "email": "hello@miguelramos.dev",
    "name": "miguelramos",
    "url": "https://github.com/miguelramos"
  },
  "license": "ISC",
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/@types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/@types/index.d.ts",
      "import": "./dist/index.mjs"
    }
  },
  "typesVersion": {
    "*": {
      "*": [
        "./dist/@types/index.d.ts"
      ]
    }
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:websublime/vitamin.git",
    "directory": "packages/core"
  },
  "files": [
    "dist/",
    "./LICENSE.md",
    "./README.md"
  ]
}
